import 'package:flutter/material.dart';

class SubnetScreen extends StatelessWidget {
  const SubnetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Subnet Calculator')),
      body: Center(child: Text('Subnet Screen')),
    );
  }
}
